[Unit]
Description=Renew GeekBird HTTPS certificate
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
ExecStart=/opt/geekbird-certbot/bin/certbot renew --quiet --cert-name geekbird-ip --deploy-hook "/usr/sbin/nginx -t && /bin/systemctl reload nginx"
